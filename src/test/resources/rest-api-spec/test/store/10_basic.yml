"Test m table creation and caching":

  - do:
      fs.create_table:
        name: "test"
        proportion: 0.5
        alpha: 0.1
        k: 10
        size: 10
  - do:
      fs.create_table:
        name: "test"
        proportion: 0.99
        alpha: 0.1
        k: 10
        size: 10

  - do:
      indices.exists:
        index: .fs_store

  - do:
      fs.get_table: {}

  - length: { hits.hits: 2 }
